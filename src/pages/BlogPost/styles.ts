import styled from 'styled-components';

export const Container = styled.div`
  width: 90%;
  max-width: 1080px;
  padding: 3rem 0;
  margin: 0 auto;

  h1 {
    text-align: center;
    margin-bottom: 2rem;
    color: var(--bluePurple);
  }
`;
